🎯 What: The testing gap addressed
- Missing parameterize tests that provide various invalid strings and infinity/NaN to test robustness in `validate_numeric_score`.

📊 Coverage: What scenarios are now tested
- Valid scores and numeric strings.
- Empty and missing strings.
- Invalid strings and math constants (Infinity, NaN, -Infinity).
- Out-of-bounds parameters.
- Valid / invalid step parameter numbers.

✨ Result: The improvement in test coverage
- Introduced `backend/tests/test_exams_support.py` with parameterized test variables for 22 different input variants across valid format checks, bound limits, NaN/Infinity cases, step validation, missing scores, etc. Added a native `is_nan()` and `is_infinite()` check for Decimal types in the source file `backend/app/services/exam_support.py`.
